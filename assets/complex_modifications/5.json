{
  "title": "caps lock を ctrl にする",
  "rules": [
    {
      "description": "caps lock を ctrl にする",
      "manipulators": [
        {
          "type" : "basic",
          "from" : {
            "key_code": "caps_lock"
          },
          "to": [{
            "key_code": "right_control"
          }]
        }]
    }
  ]
}
